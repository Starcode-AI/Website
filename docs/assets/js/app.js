import{config}from"./config.js";import{setup,text,lang}from"./i18n.js";import{setupTheme}from"./theme.js";
const toast=document.querySelector(".toast");function unavailable(){toast.textContent=text("unavailable");toast.classList.add("show");clearTimeout(window.tt);window.tt=setTimeout(()=>toast.classList.remove("show"),2200)}function card(x){const e=document.createElement(x.url?"a":"button");e.className="card reveal"+(x.url?"":" disabled");e.style.setProperty("--accent",x.accent);if(x.url){e.href=x.url;e.target="_blank";e.rel="noopener noreferrer"}else{e.type="button";e.onclick=unavailable}e.innerHTML=`<span class="card-top"><span class="card-icon">${x.icon}</span><span class="arrow">${x.url?"↗":"·"}</span></span><span><h3>${x.title}</h3><p data-card="${x.key}"></p></span>`;return e}function cards(){for(const g of["project","social","support"]){const box=document.querySelector(`[data-grid="${g}"]`);config.links.filter(x=>x.group===g).forEach(x=>box.append(card(x)))}update()}function update(){document.querySelectorAll("[data-card]").forEach(e=>e.textContent=text(`cards.${e.dataset.card}`,lang()))}function reveal(){const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");o.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll(".reveal").forEach((e,i)=>{e.style.animationDelay=`${Math.min(i*55,330)}ms`;o.observe(e)})}cards();setupTheme();setup();reveal();document.querySelectorAll("[data-email]").forEach(e=>e.href=`mailto:${config.email}`);document.querySelectorAll("[data-year]").forEach(e=>e.textContent=new Date().getFullYear());addEventListener("langchange",update);addEventListener("scroll",()=>document.querySelector(".header").classList.toggle("scrolled",scrollY>12),{passive:true});
